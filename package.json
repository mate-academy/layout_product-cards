{
  "name": "product-cards",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html",
    "format": "prettier --write './src/**/*.{html,css,scss}'",
    "lint": "npm run format && stylelint 'src/**/*.{scss,css}'",
    "test": "npm run lint",
    "build": "parcel build src/index.html",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@mate-academy/stylelint-config": "^0.0.15",
    "@parcel/resolver-default": "^2.14.4",
    "@parcel/transformer-sass": "^2.14.4",
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.4",
    "prettier": "^3.3.2",
    "stylelint": "^16.7.0",
    "stylelint-scss": "^6.4.1"
  }
}
