{
  "name": "layout-product-cards",
  "source": "src/index.html",
  "scripts": {
    "test": "npm run lint && npm run test:only",
    "start": "parcel --open",
    "build": "parcel build --public-url=/dist/"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "backstopjs": "^6.3.25",
    "parcel": "^2.12.0"
  }
}
