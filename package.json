{
  "name": "layout_product-cards",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html",
    "test": "cypress run",
    "test:report": "cypress run --reporter mochawesome --reporter-options reportDir=report/html_report,overwrite=true,html=true,json=false"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "cypress": "^13.17.0",
    "mochawesome": "^7.1.3",
    "parcel": "^2.9.3",
    "sass": "^1.70.0"
  }
}
