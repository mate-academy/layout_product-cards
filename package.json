{
  "name": "product-cards",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "npm run lint && npm run test:only",
    "lint": "npm run format && mate-scripts lint",
    "format": "npx prettier --ignore-path .prettierignore --write './src/**/*.{html,css,scss}'",
    "test:only": "mate-scripts test",
    "reference": "mate-scripts test -- --reference"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.0"
  }
}
